/**
 * 雷达图 半径轴 (yAxis) 默认配置
 */
export const Y_AXIS_OPTIONS = {
  line: null,
  tickLine: null,
  grid: {
    line: {
      type: 'line',
      style: {
        lineDash: null,
      },
    },
  },
};

/**
 * 雷达图 角度轴 (xAxis) 默认配置
 */
export const X_AXIS_OPTIONS = {
  line: null,
  tickLine: null,
  grid: {
    line: {
      style: {
        lineDash: null,
      },
    },
  },
};
